# ТЗ облачному агенту: игра Маши «Прятки с Безликом»

Дата передачи: 25.09.2026. Работу ведёт облачная сессия Claude Code, код — в этом репозитории.
Прежний агент работал на Маке владельца; всё его состояние здесь. Порядок чтения:
этот файл → `README.md` (устройство кода, правила игры) → `BRIEF-stage1.md` (исходное ТЗ Маши).

---

## 0. С кем работаешь и как говорить

- Игру делает **Маша** (ребёнок) вместе с папой — владельцем. Пишут голосом, сумбурно, по-русски.
- Отвечай **простыми словами**, без программистского жаргона. Не «рефакторинг контроллера»,
  а «Моти теперь разгоняется медленнее, как тяжёлый».
- После каждой выкладки — ссылка **https://250bar.ru/masha/** и 3–5 пунктов «что поменялось, как проверить».
- Не проверил — так и скажи. Упало — покажи чем.
- Персонажи — образы студии Ghibli (Безлик, Кот-автобус…). Для домашней игры ок; если зайдёт речь
  о публикации/продаже — предупреди, что нужны свои персонажи.

## 1. Первым делом (один раз за сессию)

```bash
bash tools/cloud/setup.sh      # ключ из MASHA_SSH_KEY_B64, закреплённый отпечаток сервера, проверка входа
```

Дальше выкладка одной командой: `./deploy.sh` (откат: `./deploy.sh rollback`).

- Ключ облака пускает **только Linux-пользователя `masha`** на `185.250.44.94`. Он может менять лишь
  `/var/www/masha-game/` — это и есть «пространство Маши». Больше на сервере ничего не трогаешь и не можешь.
- На том же сервере боевые сайты владельца (250bar.ru, интент.рф). **Никаких root-действий**,
  nginx/SSH/файрвол не менять. Если что-то требует root — напиши владельцу, что и зачем.
- Если `setup.sh` не пустил: 1) окружению не открыт выход в интернет / порт 22 — владельцу выбрать
  сетевой доступ «полный» (или разрешить 185.250.44.94) в настройках окружения; 2) ключ ещё не
  добавлен на сервер; 3) переменная обрезалась. Скажи, какая из трёх.
- Если выкладки нет, всё равно можно работать: коммить в ветку, владелец выложит с Мака.

## 2. Git

- Рабочая ветка: **`cloud/<тема>`** от `main` (например `cloud/masha-abilities`). В `main` не пушь —
  владелец мерджит сам. Коммиты по-русски, коротко: что поменялось для игрока.
- В git никогда не класть ключи и пароли. Ключи живут только в переменных окружения.

## 3. Как устроено (коротко; подробно — README.md)

- Чистые ES-модули + Three.js r186 (`vendor/three.min.js`). **Сборки нет**, npm не нужен.
- Локальный просмотр: `python3 -m http.server 8080` из корня → http://localhost:8080.
  Браузер держит старые модули в кеше — открывай с жёсткой перезагрузкой или сервером с `Cache-Control: no-store`.
- Перед выкладкой `deploy.sh` сам делает `node --check` всех модулей.
- Отладка из консоли браузера: `window.__game` — весь объект игры (`__game.agents`, `__game.ghosts`,
  `__game.beginRound('watch')`, `__game.selectHero('moti')`).

| Что | Где |
|---|---|
| Все числа (физика героев, Безлик, умения, боты) | `src/config/config.js` |
| Список героев, карточки, скины | `src/characters/index.js` |
| Модели героев (собраны из примитивов кодом) | `src/characters/{masha,catbus,moti,noface}.js` |
| Физика движения (общая для игрока и ботов) | `src/player/controller.js` |
| Мозг ботов | `src/player/bot.js` |
| Безлик: зрение, слух, рывок, оглушение | `src/enemies/ghost.js`, пути — `pathfinder.js` (A*) |
| Умения: что умеет герой, клавиши, срабатывание | `src/abilities/abilities.js`, эффекты — `fx.js` |
| Раунд целиком | `src/game/game.js` |
| Интерфейс, панель физики ⚙/F2 | `src/ui/ui.js`, `src/ui/tuner.js` |
| Карта | `src/world/map.js`, столкновения — `colliders.js` |

**Принцип, который держим:** игрок и боты — один и тот же `PlayerController`, отличается только
источник «ввода» (клавиатура или `BotBrain`). Новые умения/физику делай так, чтобы ими пользовались
и боты (см. `AbilitySet.botThink`).

## 4. Что уже сделано (проверено в браузере, ПК и телефон)

1. Экран выбора как на Машиных картинках: карточки, скины Моти, число Безликов 1–3, боты вкл/выкл,
   кнопка «👀 Смотреть, как бегают все».
2. Три героя со **своей физикой** (таблица в README): Маша лёгкая и резкая, НэкоБус быстрый, но
   инерционный, Моти тяжёлый. У всех рывок **E**. Панель ⚙ / F2 — ползунки вживую + «Скопировать в config».
3. Безлик: видит только напрямую, в кустах не видит, слышит бег, рывок вблизи, 1–3 штуки.
4. Боты: гуляют, прячутся в кусты, убегают с рывком, Моти-бот сам колдует.
5. **Дядюшка Моти по развороту персонажа** (картинку разворота владелец доложит в `refs/moti-sheet.*`; пока в репо только `refs/1.webp`, `refs/2.webp` — карточки выбора): рюкзак-домик, фонарь
   с лапкой, огоньки, 4 скина; умения **1** Уютный приют, **2** Тёплый свет, **3** Духи-помощники,
   **4** Путь фонарей, **F** удар фонарём, **G** приветствие — клавишами и кликом по панели.
6. Телефон: стик, камера пальцем, кнопки прыжка/бега/рывка, умения столбиком справа.

## 5. Очередь задач (по порядку)

**5.1 Развороты остальных героев.** Владелец будет присылать картинки-«развороты» как у Моти
(вид спереди/сбоку/сзади, габариты, 4 способности, анимации, скины). По каждому:
- модель: силуэт, цвета, аксессуары как на развороте; рост — из блока «Габариты» (игрок = 1,7 м);
- 4 способности на клавишах **1–4** + атака **F** + эмоция **G**, у каждой — эффект, перезарядка
  в `CONFIG.abilities`, иконка на панели, поведение бота в `botThink`;
- анимации из блока «Специальные анимации» (через `st.action` в `update()` модели, см. `moti.js`);
- скины, если есть на развороте (см. `MOTI_SKINS`).
Ждём: Маша, НэкоБус, Безлик. **Пока разворота нет — не выдумывай умения**, спроси.

**5.2 Способности в духе прятки.** Сейчас умения есть только у Моти. Для Маши и НэкоБуса до
разворотов можно предложить (не делать без «да»): Маша — «тихие шаги» (бег не слышно 4 с);
НэкоБус — «подвезти» (друг рядом получает скорость).

**5.3 Баланс физики по отзывам Маши.** Она крутит ползунки ⚙ и присылает строку из «Скопировать
в config» — вставь в `CONFIG.heroes` / `CONFIG.ghost`, выложи.

**5.4 Мир по фото (World Labs Marble).** Когда владелец пришлёт экспорт мира из Marble (`.spz`/`.ply`
+ collider mesh `.glb`): подключить рендер Spark (three.js, MIT) как вторую карту, столкновения —
из collider mesh (заменить прямоугольники/круги `CollisionWorld` на проверку по мешу или
упрощённо — высотная карта + стены), точки появления, кусты-укрытия. Вес для телефона — лёгкий
вариант (~500 тыс. сплатов). До получения файла — не начинать.

**5.5 Модели .glb (позже).** Если герои придут из Meshy/Tripo/Hunyuan3D с «костями» — грузить
через GLTFLoader в `assets/characters/`, оставив тот же интерфейс `{root, update(dt, st), height}`.

## 6. Известные недочёты (можно чинить по ходу)

- Камера за Моти поднята и сдвинута «через плечо» (`cam.side`) — глазами в браузере ещё не проверено.
- ИСПРАВЛЕНО 25.09: Безлик застревал у стен; боты сидели в кусте до конца; Моти-бот не выручал друзей; рывок кадра при появлении Безлика (прогрев шейдеров).

## 6.1 Симуляция баланса без браузера

`node --import ./tools/sim/register.mjs tools/sim/sim.mjs 10 2` — 10 раундов «Смотреть» с 2 Безликами
за ~10 с. Печатает, кого сколько ловят, кто выжил, сколько раз применялись умения. Меняешь физику или
поведение — сравни цифры до/после. На 25.09 (2 Безлика, 10 раундов): поймали НэкоБуса 9, Машу 8, Моти 4.

## 7. Проверка перед выкладкой (минимум)

1. Страница грузится без ошибок в консоли; экран выбора; все 3 героя переключаются.
2. «Играть» → бег, прыжок, рывок; через 10 с появляется Безлик и гонится.
3. «Смотреть» с 3 Безликами — боты бегают, пойманные исчезают, раунд заканчивается.
4. Узкий экран (375×812): кнопки не налезают друг на друга, кнопки умений нажимаются.
5. `./deploy.sh` → открыть https://250bar.ru/masha/ и повторить п.1.
